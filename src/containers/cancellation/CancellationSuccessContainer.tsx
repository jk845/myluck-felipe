import React from 'react';
import { CancellationSuccessPresenter } from '@/components/presenters/cancellation';

export const CancellationSuccessContainer: React.FC = () => {
  // This is a simple container with no logic
  // It just renders the presenter component
  return <CancellationSuccessPresenter />;
};